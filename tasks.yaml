# yaml-language-server: $schema=https://raw.githubusercontent.com/victor-software-house/qctl/main/schema/tasks.schema.json
schema_version: 4
prefix: UXD
active: UXD-001
queue:
  - id: UXD-001
    title: Prove native socket activation on macOS and Linux
    scope: activation
    outcome: A daemon receives its socket on stdin from launchd and from systemd --user, exits idle, and is relaunched by the next connection, before any crate code depends on that path.
    blocked_by: []
    acceptance:
      - The three scenarios in openspec/changes/native-socket-activation/specs/activation hold on macOS and on Linux.
      - The design doc records the result and answers its open question 2.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md

  - id: UXD-002
    title: Extract the consumer cache into unixd-cache
    scope: unixd-cache
    outcome: unixd-cache stores any serializable value under caller-built keys with fresh and stale horizons, per-key locks, atomic writes, and bounded prune, with no async runtime.
    blocked_by:
      - UXD-001
    acceptance:
      - Verification steps 2, 5, 6, and 8 in the design doc pass.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md

  - id: UXD-003
    title: Extract framing, envelopes, the peer check, and the client into unixd
    scope: unixd
    outcome: unixd carries versioned newline-delimited JSON frames with a size cap over a handler trait, and closes a connection from another user before reading.
    blocked_by:
      - UXD-001
    acceptance:
      - Verification steps 3 and 4 in the design doc pass.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md

  - id: UXD-004
    title: Add the stdin listener, unit install, drain, and idle exit
    scope: unixd
    outcome: unixd installs and removes its own launchd and systemd units, takes its socket on stdin, drains on every stop path, and exits idle without unlinking the socket.
    blocked_by:
      - UXD-003
    acceptance:
      - Verification steps 7 and 10 in the design doc pass on macOS and Linux.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md

  - id: UXD-005
    title: Add the coalescing seam and the cross-mode equivalence test
    scope: unixd
    outcome: The daemon coalesces concurrent identical requests in process, the direct path binds a no-op, and both paths give the same output and cache state.
    blocked_by:
      - UXD-002
      - UXD-004
    acceptance:
      - Verification step 9 in the design doc passes.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md

  - id: UXD-006
    title: Move the first consumer onto unixd
    scope: adoption
    outcome: The first consumer uses unixd and unixd-cache in place of its own daemon, transport, and cache, and its self-spawn code is deleted.
    blocked_by:
      - UXD-005
    acceptance:
      - The consumer's own suite passes on the published crates, and it contains no start lock or self-spawn code.
    links:
      - https://github.com/victor-software-house/unixd/blob/main/docs/design/daemon-and-cache.md
archive: []
horizon: []
